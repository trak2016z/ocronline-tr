{% extends "/layout.html.twig" %}

{% block body %}
<div class="container">
<h1>Dokumenty użytkownika <b>{{user.username}}</b></h1>
    <div class="row">
        <div class="col-xs-12">
            
            <div class="card-columns">
                    {% for document in documents %}
                        {{ doc.entry(document) }}
                    {% endfor %}
            </div>
        </div>
    </div>
</div>
{% endblock %}